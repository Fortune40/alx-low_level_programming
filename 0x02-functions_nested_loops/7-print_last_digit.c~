#include "main.h"

/**
 * print_last_digit -> prints last digit of a number
 * @n: input number as an integer
 * Return: last digit
 */
int print_last_digit(int)
{
if (n < 0)
n = -n;
return (n % 10);
}
