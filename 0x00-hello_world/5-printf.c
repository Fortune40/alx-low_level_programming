#include <stdio.h>

/**
 * main - prints strings of character
 * Return: zero if sucess, non-zero if fail
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
