#include <stdio.h>

/**
 * main - prints out string character
 * Return: 0 if exited properly, otherwise non-zero
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
